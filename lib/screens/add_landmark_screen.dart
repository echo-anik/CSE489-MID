import 'package:flutter/material.dart';

class AddLandmarkScreen extends StatelessWidget {
  const AddLandmarkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Landmark'),
      ),
      body: const Center(
        child: Text('Add Landmark Screen - Implementation pending'),
      ),
    );
  }
}
